# Decisions

## Uso del documento
- Registrar decisiones activas que cambien enfoque, alcance o forma de trabajo.
- Mantener un identificador unico por decision.
- No eliminar decisiones obsoletas; marcarlas como sustituidas cuando aplique.

## Registro de decisiones

| ID | Fecha | Decision | Justificacion | Estado |
| --- | --- | --- | --- | --- |
| D-001 | 2026-04-20 | La documentacion minima del proyecto vivira bajo `/docs` en las carpetas `00-governance`, `01-product` y `04-codex`. | Centraliza el control del proyecto sin tocar codigo funcional. | Vigente |
| D-002 | 2026-04-20 | El trabajo se realizara por etapas aprobadas. | Reduce ambiguedad, evita cambios prematuros y facilita control de alcance. | Vigente |
| D-003 | 2026-04-20 | Los estatus se manejaran por modulo. | El contexto del sistema indica comportamiento diferenciado segun el modulo. | Vigente |
| D-004 | 2026-04-20 | Un proyecto cerrado permanecera en historico y ya no generara alertas. | Preserva trazabilidad sin mantener actividad operativa innecesaria. | Vigente |
| D-005 | 2026-04-20 | Se contara con un catalogo reutilizable de contactos internos y externos entre proyectos. | Permite reutilizacion de relaciones y continuidad operativa. | Vigente |
| D-006 | 2026-04-20 | Una donacion puede tener multiples aplicaciones. | Regla de negocio explicita del contexto actual. | Vigente |
| D-007 | 2026-04-20 | STAGE-01 usara una estructura backend en cuatro proyectos: `FMCPA.Api`, `FMCPA.Domain`, `FMCPA.Application` y `FMCPA.Infrastructure`. | Permite crecer por etapas sin refactor mayor y mantiene separacion clara entre API, contratos, dominio e infraestructura. | Vigente |
| D-008 | 2026-04-20 | STAGE-01 usara una entidad tecnica neutral `SystemSetting` para validar wiring de EF Core y SQL Server sin introducir negocio. | Deja lista la persistencia para futuras migraciones sin anticipar tablas funcionales. | Vigente |
| D-009 | 2026-04-20 | La integracion minima de foundation se validara con un endpoint JSON `/health` y con frontend local apuntando a `http://localhost:5080`. | Es la verificacion mas pequena y segura para comprobar wiring entre frontend y backend antes de servicios funcionales. | Vigente |
| D-010 | 2026-04-20 | La solucion backend canonica del repositorio sera `FMCPA.Backend.sln`. | En este entorno fue la variante estable para restore y build de la solucion completa; evita ambiguedad con formatos alternos. | Vigente |
| D-011 | 2026-04-20 | STAGE-02 modelara la base compartida con `Contact`, `ContactType`, `ContactParticipation`, `CommissionType`, `EvidenceType` y `ModuleStatusCatalogEntry`. | Permite reutilizacion transversal sin anticipar aun entidades de negocio como Mercado, Donacion, Oficio o Gestion. | Vigente |
| D-012 | 2026-04-20 | La semilla inicial de estatus por modulo se limitara a `ACTIVE` y `CLOSED` por modulo dentro del alcance actual. | Cubre la necesidad reusable minima sin sobredefinir ciclos de vida que corresponden a etapas posteriores. | Vigente |
| D-013 | 2026-04-20 | `PlatformDbContextFactory` respetara `ConnectionStrings__PlatformDatabase` antes de usar la cadena de diseno por defecto. | Permite generar y aplicar migraciones contra instancias locales temporales sin editar archivos de configuracion del repositorio. | Vigente |
| D-014 | 2026-04-20 | STAGE-03 reutilizara `Contact` como vinculo opcional para secretario general y locatario, manteniendo snapshot local de nombre y datos operativos dentro del modulo Mercados. | Reduce retrabajo futuro y preserva trazabilidad historica aun si el catalogo compartido cambia despues. | Vigente |
| D-015 | 2026-04-20 | `ModuleStatusCatalogEntry` incorporara `ContextCode` y `ContextName` para distinguir catalogos de estatus dentro del mismo modulo. | Permite modelar estatus de Mercado y de Incidencia sin crear catalogos paralelos ni romper la base reusable de STAGE-02. | Vigente |
| D-016 | 2026-04-20 | La cédula digitalizada del locatario se resolvera en STAGE-03 con almacenamiento local configurable y metadatos en base de datos, sin generalizar aun un sistema transversal de documentos. | Cumple el alcance del modulo con cambios acotados y evita sobredisenar una capacidad global antes de que otros modulos la requieran. | Vigente |
| D-017 | 2026-04-20 | STAGE-04 mantendra `Donation` como registro maestro separado de `DonationApplication`, y el estatus maestro no cerrado se recalculara automaticamente segun el monto aplicado acumulado. | Respeta la regla base de una donacion con multiples aplicaciones y evita inconsistencias entre porcentaje aplicado y estatus maestro. | Vigente |
| D-018 | 2026-04-20 | STAGE-04 reutilizara `Contact` como vinculo opcional del responsable de aplicacion, manteniendo snapshot local del nombre del responsable dentro de la aplicacion. | Permite trazabilidad historica y reutilizacion del catalogo compartido sin depender de que el contacto cambie en el tiempo. | Vigente |
| D-019 | 2026-04-20 | La evidencia de Donatarias se resolvera en STAGE-04 con almacenamiento local configurable por modulo y metadatos en base de datos, reutilizando `EvidenceType` pero sin abrir aun un sistema documental transversal. | Cumple el alcance de evidencias por aplicacion con cambios acotados y evita adelantar una capacidad global no aprobada. | Vigente |
| D-020 | 2026-04-20 | STAGE-04 bloqueara el registro de aplicaciones que excedan el monto remanente de la donacion. | Evita porcentajes aplicados mayores a 100 y mantiene coherencia operativa minima mientras no exista un flujo formal de ajustes o sobreejercicio. | Vigente |
| D-021 | 2026-04-20 | STAGE-05 no abrira aun una entidad compartida `FinancialInstitution`; `FinancialPermit` conservara `FinancialName`, `InstitutionOrDependency` y `PlaceOrStand` como snapshot operativo. | Evita normalizacion prematura mientras el alcance aprobado solo requiere oficios, vigencias, stands y creditos por permiso. | Vigente |
| D-022 | 2026-04-20 | STAGE-05 reutilizara `Contact` como vinculo opcional para promotor, beneficiario y destinatario de comision, manteniendo snapshot local de nombres y telefonos dentro de `FinancialCredit` y `FinancialCreditCommission`. | Preserva trazabilidad historica y permite seguir operando aunque el catalogo compartido cambie despues. | Vigente |
| D-023 | 2026-04-20 | Las comisiones de STAGE-05 permaneceran acotadas al contexto de `FinancialCredit` y se distinguiran con `RecipientCategory` entre `COMPANY`, `THIRD_PARTY` y `OTHER_PARTICIPANT`, sin abrir aun un consolidado global de comisiones. | Cumple el alcance del modulo con multiples comisiones por credito y evita mezclar todavia Financieras con Federacion o con una vista transversal no aprobada. | Vigente |
| D-024 | 2026-04-20 | STAGE-06 mantendra el modulo Federacion separado de Donatarias y Financieras mediante entidades propias para gestiones, donaciones, aplicaciones, comisiones y evidencias. | Evita mezclar contextos operativos distintos antes de abrir el historico o el consolidado transversal final. | Vigente |
| D-025 | 2026-04-20 | Los participantes de gestion en STAGE-06 reutilizaran `Contact` como referencia obligatoria, pero conservaran snapshot de nombre, organizacion y cargo dentro de `FederationActionParticipant`. | Preserva trazabilidad historica y permite multiples participantes internos y externos sin depender de que el catalogo compartido cambie en el tiempo. | Vigente |
| D-026 | 2026-04-20 | La evidencia de Federacion se resolvera en STAGE-06 con almacenamiento local configurable por modulo y metadatos en base de datos, reutilizando `EvidenceType` sin abrir aun un sistema documental transversal. | Cumple el alcance de evidencias por aplicacion con cambios acotados y mantiene el mismo patron tecnico ya probado en Mercados y Donatarias. | Vigente |
| D-027 | 2026-04-20 | Las comisiones de STAGE-06 se registraran a nivel de aplicacion de donacion de Federacion, reutilizando `CommissionType` y `RecipientCategory`, sin abrir aun el consolidado transversal global. | Permite trazabilidad suficiente dentro del modulo y deja la consolidacion final para una etapa posterior aprobada. | Vigente |
| D-028 | 2026-04-20 | STAGE-07 construira la vista transversal de comisiones como una capa de consulta simple sobre Financials y Federacion, sin rehacer sus modelos internos. | Cumple el cierre operativo del MVP con filtros utiles y contratos estables, evitando refactor transversal prematuro sobre modulos ya operativos. | Vigente |
| D-029 | 2026-04-20 | La bitacora MVP de STAGE-07 se resolvera como una vista derivada de registros operativos ya capturados, enfocada en eventos de alta, vinculacion, aplicacion, evidencia y comision. | Permite visibilidad minima util sin introducir auditoria avanzada, event sourcing ni nuevas tablas de negocio en la etapa de cierre. | Vigente |
| D-030 | 2026-04-20 | La consulta historica MVP mostrara registros cerrados o archivados usando el ultimo timestamp conocido del registro (`UpdatedUtc` o `CreatedUtc`) mientras no exista un evento de cierre dedicado. | Permite consulta historica usable con cambios acotados y deja explicita la limitacion para un endurecimiento post-MVP si se requiere auditoria de cierre real. | Vigente |
| D-031 | 2026-04-20 | `Track 1` introducira una entidad transversal minima `AuditEvent` para registrar eventos nuevos hacia adelante y cierres formales, sin convertir el sistema en event sourcing ni auditoria forense completa. | Reduce una reserva central del MVP con el menor cambio estructural posible y sin rehacer el modelo funcional ya implementado. | Vigente |
| D-032 | 2026-04-20 | El cierre formal se modelara como operacion explicita por entidad cerrable y su fecha valida para historico sera la del evento de bitacora; si el registro previo no tiene ese evento, historico usara fallback al ultimo timestamp conocido. | Mejora trazabilidad de cierre y mantiene compatibilidad con el historial heredado del MVP sin exigir retroconstruccion masiva de eventos anteriores. | Vigente |
| D-033 | 2026-04-20 | El hardening documental de `Track 1` introducira una entidad ligera `StoredDocument` para registrar metadatos homogeneos de los archivos ya existentes y de los uploads nuevos, sin abrir todavia una plataforma documental transversal completa. | Reduce el riesgo de metadatos huerfanos y permite verificacion operativa de integridad con un cambio estructural acotado y compatible con los modulos actuales. | Vigente |
| D-034 | 2026-04-20 | La migracion de `StoredDocument` hara backfill minimo desde Mercados, Donatarias y Federacion, y los downloads seguiran aceptando fallback a la metadata del modulo cuando el registro transversal no exista. | Permite endurecer el storage desde ahora sin exigir una regularizacion compleja ni romper documentos ya cargados durante el MVP. | Vigente |
| D-035 | 2026-04-21 | La convencion local estandar de desarrollo usara SQL Server en Docker sobre `127.0.0.1:14333`, backend en `127.0.0.1:5080`, frontend en `127.0.0.1:4200`, base `FMCPA_Development` y storage bajo `App_Data/` del repositorio. | Reutiliza los puertos y validaciones ya usados durante el MVP y reduce friccion para futuras sesiones sin tocar produccion ni CI/CD. | Vigente |
| D-036 | 2026-04-21 | La operacion local repetible se resolvera con `docker-compose.local.yml`, `.env.local.example` y scripts `scripts/local/*`, manteniendo overrides por variables de entorno en lugar de reconfigurar profundamente la aplicacion. | Estandariza arranque local, migraciones y smoke checks con el menor cambio posible y preserva compatibilidad con el wiring actual del backend y frontend. | Vigente |
| D-037 | 2026-04-21 | El hardening operativo local incorporara un `doctor.sh` no destructivo con salida `OK/WARNING/ERROR` y un `reset-db.sh` destructivo solo para la base configurada en `FMCPA_DB_NAME`, bloqueado sin confirmacion o `--force`. | Agrega recuperacion y diagnostico local sin tocar produccion, evita operaciones destructivas por defecto y mantiene acotado el alcance al contenedor y base del proyecto. | Vigente |
| D-038 | 2026-04-21 | El smoke funcional del MVP se implementara como `scripts/local/smoke-mvp.sh`, separado de `smoke.sh`, creando datos tecnicos minimos via API y validando solo la capa operativa esencial del MVP. | Mantiene un smoke corto, repetible y mantenible sin convertirlo en una suite pesada ni exigir frontend, uploads o navegacion UI para detectar regresiones tempranas. | Vigente |
| D-039 | 2026-04-21 | El wiring local Angular -> API para desarrollo se resolvera con `apiBaseUrl` relativo en `environment.development.ts` y un proxy local generado por `run-frontend.sh` a partir de `FMCPA_API_PORT`. | Permite cambiar el puerto local del backend sin editar codigo fuente manualmente, mantiene intacto el build de produccion y evita refactorizar los servicios HTTP existentes. | Vigente |
| D-040 | 2026-04-21 | El repositorio versionara un manifiesto local `.config/dotnet-tools.json` con `dotnet-ef` 10.0.6 y los scripts de migracion usaran esa herramienta local en lugar de depender de una version global variable. | Elimina la advertencia `10.0.3` vs `10.0.6`, hace el tooling reproducible y mantiene el ajuste dentro del alcance local de desarrollo. | Vigente |
| D-041 | 2026-04-21 | El flujo corto de trabajo local se resolvera con `dev-up.sh` y `dev-down.sh`, que gestionan solo recursos levantados por el proyecto y almacenan estado temporal bajo `FMCPA_LOCAL_STATE_DIR`. | Reduce friccion operativa sin crear un orquestador complejo ni matar procesos ajenos al proyecto. | Vigente |
| D-042 | 2026-04-21 | La regularizacion retrospectiva del historico legado se representara con `AuditEvent` usando `ActionType = LEGACY_CLOSE_NORMALIZED`, `IsCloseEvent = true`, `OccurredUtc` tomado del mejor timestamp legado disponible y metadata explicita del origen retrospectivo. | Permite reducir la dependencia de `LEGACY_TIMESTAMP_FALLBACK` sin falsear esos eventos como cierres operativos reales y sin introducir un esquema nuevo ni una migracion innecesaria. | Vigente |
| D-043 | 2026-04-21 | La jerarquia de historico quedara definida como `FORMAL_CLOSE_EVENT` primero, `LEGACY_CLOSE_NORMALIZED` despues y `LEGACY_TIMESTAMP_FALLBACK` solo como ultimo recurso, y la regularizacion no se ejecutara automaticamente al arrancar la aplicacion. | Mantiene separada la trazabilidad operativa real de la reconstruccion retrospectiva, hace el proceso controlable e idempotente y evita efectos secundarios no deseados en cada arranque. | Vigente |
| D-044 | 2026-04-21 | El endurecimiento de transiciones se resolvera con una capa minima `StateTransitionSupport` en API, sin introducir una state machine completa ni reestructurar el dominio. | Centraliza reglas y mensajes de cierre o mutacion invalida con el menor cambio posible y evita duplicar logica en los endpoints. | Vigente |
| D-045 | 2026-04-21 | Se permitira el cierre formal de `Donation`, `FinancialPermit`, `FederationAction` y `FederationDonation` desde estados no terminales aunque el resultado operativo no haya quedado totalmente aplicado o satisfactorio, pero se bloquearan cierres sobre estados ya terminales y mutaciones nuevas sobre padres cerrados o archivados. | Respeta el criterio operativo de “carpetazo” documentado, endurece coherencia minima de alertas/historico y evita contaminar la bitacora con intentos invalidos. | Vigente |
| D-046 | 2026-04-21 | `Track 2` abrira la autenticacion minima con una entidad local `ApplicationUser`, hash `PBKDF2-SHA256` y `JWT bearer`, protegiendo toda la superficie `/api` salvo `/api/auth/login`, `/health` y la raiz del servicio. | Cumple el objetivo de exigir autenticacion con un cambio estandar, acotado y extensible, sin introducir todavia proveedores externos ni autorizacion fina por roles o claims. | Vigente |
| D-047 | 2026-04-21 | El usuario bootstrap local se aprovisionara solo en `Development` desde configuracion local (`Auth__Bootstrap__*` / `FMCPA_AUTH_BOOTSTRAP_*`), sin versionar contrasenas reales en el repositorio. | Permite disponer de un usuario inicial controlado para desarrollo y smoke sin persistir secretos reales ni abrir todavia gestion completa de usuarios. | Vigente |
| D-048 | 2026-04-21 | El frontend almacenara la sesion minima actual en `sessionStorage`, protegera navegacion con guard, enviara el bearer por interceptor y resolvera descargas sensibles via `HttpClient` autenticado en vez de URLs abiertas. | Reduce exposicion innecesaria dentro del alcance actual, evita dejar rutas principales libres y mantiene una base razonable para evolucion futura sin redisenar el shell del MVP. | Vigente |
| D-049 | 2026-04-21 | La primera capa de autorizacion de `Track 2` se resolvera con tres roles fijos `ADMIN`, `OPERATOR` y `READONLY`, persistidos en `ApplicationUser.RoleCode` y emitidos como claim de rol en JWT. | Permite diferenciar alcance funcional sin introducir todavia una plataforma completa de identidad ni un RBAC fino por accion o modulo. | Vigente |
| D-050 | 2026-04-21 | La API usara tres politicas estables `read`, `write` y `admin`: `READONLY/OPERATOR/ADMIN` para lectura, `OPERATOR/ADMIN` para escritura funcional y `ADMIN` para cierres formales y endpoints administrativos actuales. | Mantiene el cambio pequeno, mantenible y auditable, reaprovecha los grupos de endpoints ya existentes y evita granularidad excesiva en esta etapa. | Vigente |
| D-051 | 2026-04-21 | El bootstrap local de `Track 2` mantendra `admin` como usuario primario obligatorio por convension y agregara usuarios opcionales `operator` y `readonly` solo si sus passwords locales se configuran fuera del repo. | Facilita validacion local de permisos por rol sin versionar secretos reales ni abrir todavia gestion de usuarios. | Vigente |
| D-052 | 2026-04-21 | La migracion que introduce `ApplicationUser.RoleCode` rellenara usuarios preexistentes con `READONLY` como valor seguro por defecto. | Evita escalacion accidental de privilegios durante el backfill y permite que el bootstrap local sincronice despues los roles esperados de validacion. | Vigente |
| D-053 | 2026-04-22 | La gestion minima de usuarios internos se resolvera reutilizando `ApplicationUser` y endpoints `ADMIN` dedicados para listar, consultar, crear, cambiar rol, activar/desactivar logicamente y resetear password, sin abrir todavia borrado fisico, self-service ni recuperacion avanzada. | Cumple el objetivo de administracion interna con el menor cambio posible sobre la foundation ya aprobada y evita abrir una plataforma completa de cuentas antes de tiempo. | Vigente |
| D-054 | 2026-04-22 | Los cambios sensibles de usuario (`roleCode`, `isActive`, reset administrativo de password) rotaran un `SecurityStamp` persistido y cada request autenticado validara ese stamp contra la base para invalidar tokens previos de inmediato. | Evita que un usuario desactivado o con rol/password cambiados siga operando con un bearer viejo y mantiene la solucion dentro del esquema JWT actual sin introducir sesiones servidor complejas. | Vigente |
| D-055 | 2026-04-22 | La gestion minima de usuarios exigira un password inicial o reseteado de al menos 12 caracteres y bloqueara cualquier cambio que deje al sistema sin ningun `ADMIN` activo. | Introduce una barrera minima de seguridad coherente con el track y evita una perdida accidental de administracion del sistema local sin inventar una politica de password compleja. | Vigente |
| D-056 | 2026-04-23 | La validacion runtime de la gestion minima de usuarios se cerrara sobre un stack local aislado, con SQL Server efimero, base y puertos dedicados, y usuarios bootstrap temporales solo para la corrida de validacion. | Hace reproducible la comprobacion real de `ADMIN`, `OPERATOR` y `READONLY` sin contaminar entornos previos, sin depender de `.env.local` persistente y sin abrir funcionalidad nueva fuera del alcance aprobado. | Vigente |
| D-057 | 2026-05-04 | La invalidacion real de sesion/token se mantendra sobre el `SecurityStamp` ya persistido y la validacion viva del usuario en backend, sin agregar migracion, refresh tokens, blacklist distribuida ni gestion avanzada de sesiones. | Cierra el riesgo de tokens viejos ante desactivacion, cambio de rol y reset administrativo con el menor cambio posible sobre la base existente y conserva el alcance del Track 2 minimo. | Vigente |
| D-058 | 2026-05-04 | La autorizacion minima por modulo derivara permisos desde `ApplicationUser.RoleCode`, emitira esos permisos como claims `fmcpa_permission` y los validara contra el rol vigente en cada request autenticado, sin persistir permisos por usuario. | Permite policies por superficie funcional con cambio acotado, mantiene gestion simple `rol -> permisos`, evita migracion innecesaria y conserva la invalidacion de tokens viejos cuando cambia el rol. | Vigente |
| D-059 | 2026-05-04 | Contactos se tratara como superficie operativa compartida con permisos `CONTACTS_READ` y `CONTACTS_WRITE`; cierres formales usaran `FORMAL_CLOSE_ADMIN` combinado con el permiso `*_WRITE` del modulo. | Contactos no es un catalogo administrativo y lo usan los modulos operativos; los cierres formales deben seguir fuera de `OPERATOR` sin crear permisos ultra finos como `MARKETS_CLOSE_ONLY`. | Vigente |
| D-060 | 2026-05-05 | Los uploads documentales actuales usaran una politica comun para Mercados, Donatarias y Federacion: maximo `10 MB`, extensiones `.pdf/.jpg/.jpeg/.png`, content-types `application/pdf`, `image/jpeg`, `image/png`, archivo no vacio, nombre original saneado y firma basica del contenido. | Reduce riesgo de archivos invalidos con una regla simple y mantenible, sin abrir todavia antivirus, DLP, sniffing complejo, storage externo ni una plataforma documental completa. | Vigente |
| D-061 | 2026-05-05 | Las descargas documentales se serviran solo desde los endpoints de modulo ya autorizados por permiso de lectura, con verificacion de integridad previa, `Content-Disposition` de attachment, `X-Content-Type-Options: nosniff`, `Cache-Control: no-store` y sin exponer rutas fisicas internas. | Refuerza la entrega de documentos existentes sin cambiar el storage local ni introducir permisos ultra finos por documento individual. | Vigente |
| D-062 | 2026-05-05 | El rate limiting minimo sera fijo y en memoria: `POST /api/auth/login` permitira `10` solicitudes por minuto por cliente, y `/api/admin/users` permitira `60` solicitudes por minuto por cliente/usuario autenticado. | Reduce fuerza bruta y abuso basico con un cambio acotado, sin abrir antifraude, CAPTCHA, bloqueo distribuido ni dependencia de infraestructura externa. | Vigente |
| D-063 | 2026-05-05 | El backend agregara headers HTTP simples (`nosniff`, `Referrer-Policy: no-referrer`, `X-Frame-Options: DENY`, `Permissions-Policy`) y cache `no-store` para respuestas `/api`. | Endurece el borde HTTP sin interferir con Angular local ni con las descargas ya endurecidas. | Vigente |
| D-064 | 2026-05-05 | Fuera de `Development`, la API fallara rapido si JWT usa signing key ausente o menor a 32 bytes, issuer/audience locales por defecto, lifetime no positivo, CORS wildcard o origins localhost. | Separa explicitamente tolerancias locales de configuracion no-Development y evita arranques con defaults inseguros o de desarrollo. | Vigente |
| D-065 | 2026-05-05 | La politica minima de password de `Track 2` exigira `12` caracteres, mayuscula, minuscula y numero; rechazara espacios al inicio/fin y passwords obvios locales; aplicara a bootstrap local, alta admin y reset admin. | Reduce credenciales debiles con reglas simples y configurables, sin abrir motor complejo de politicas, diccionarios externos ni breach checks. | Vigente |
| D-066 | 2026-05-05 | El lockout minimo sera por usuario con contador consecutivo: `5` fallos de login bloquean temporalmente la cuenta durante `15` minutos; login exitoso, reset admin, desbloqueo admin o expiracion de cooldown limpian el contador. | Complementa el rate limit por IP con proteccion de cuenta, mantiene persistencia minima en `ApplicationUser` y evita infraestructura distribuida compleja. | Vigente |
| D-067 | 2026-05-05 | Los eventos de autenticacion se registraran en `AuditEvent` bajo modulo `SECURITY`, sin guardar passwords ni abrir auditoria forense completa. | Da visibilidad operativa de login exitoso, fallo, lockout y desbloqueo con la bitacora existente y mantiene el alcance acotado. | Vigente |
| D-068 | 2026-05-05 | El cambio self-service de password se resolvera con un endpoint autenticado sobre el usuario actual, validando password actual, politica minima y confirmacion, y usando `ApplicationUser.ResetPassword` para rotar `SecurityStamp`. | Permite al usuario cambiar su propia credencial sin abrir recuperacion por correo, MFA, refresh tokens ni gestion avanzada de sesiones, y conserva la invalidacion real de tokens ya implementada. | Vigente |
| D-069 | 2026-05-05 | El frontend cerrara la sesion local despues de un cambio self-service exitoso y redirigira a login en vez de intentar emitir o conservar un token nuevo. | El token actual queda invalidado por rotacion de `SecurityStamp`; relogin explicito es el comportamiento mas simple y seguro para esta etapa. | Vigente |
| D-070 | 2026-05-05 | La cobertura de regresion de autorizacion se resolvera con integration tests backend ligeros usando `WebApplicationFactory`, tokens reales por `/api/auth/login` y `PlatformDbContext` InMemory. | Cubre policies reales y validacion de token sin levantar Docker/SQL Server ni introducir una suite E2E pesada o infraestructura desproporcionada. | Vigente |
| D-071 | 2026-05-05 | La matriz de regresion cubrira superficies representativas por policy y no cada endpoint individual del sistema. | Detecta regresiones reales de acceso anonimo, permisos por rol, cierres formales, administracion y self-service manteniendo el costo de mantenimiento razonable. | Vigente |
| D-072 | 2026-05-05 | El guardrail de deriva de superficie usara `docs/05-post-mvp/security-authorization-surface-guardrails.json` como manifiesto operativo machine-readable, con `security-authorization-surface-inventory.md` como inventario humano complementario. | Permite que las pruebas introspecten endpoints reales y fallen ante publicos no permitidos, `/api` sin cobertura o policies inesperadas, evitando parsers fragiles de Markdown y doble mantenimiento confuso. | Vigente |
| D-073 | 2026-05-05 | La proteccion minima de origen web se aplicara a metodos inseguros bajo `/api`: si la solicitud trae `Origin` o `Referer`, debe coincidir con `Cors:AllowedOrigins`; si trae senales browser, debe incluir `X-FMCPA-Client: FMCPA-Web`. | Endurece el flujo Angular y mutaciones sensibles sin migrar a cookies, sin rehacer JWT, sin antiforgery MVC complejo y sin romper scripts locales no-browser que no envian senales browser. | Vigente |
| D-074 | 2026-05-05 | La observabilidad minima de seguridad se expondra como endpoints `ADMIN` sobre `AuditEvent` y `ApplicationUser`, sin tablas nuevas ni agregaciones pesadas. | Da visibilidad operativa sobre eventos SECURITY, lockouts y resumen reciente reutilizando la auditoria/lockout existentes, sin abrir SIEM, antifraude avanzado ni analitica compleja. | Vigente |
| D-075 | 2026-05-05 | La primera superficie de `Track 3` sera un catalogo documental transversal sobre `StoredDocument`, con permisos de lectura derivados por modulo y descarga unificada por `documentId`, sin agregar columnas nuevas. | Reutiliza la metadata e integridad ya endurecidas, evita depender de navegar modulo por modulo y mantiene fuera versionado, retencion, backup real, OCR, storage externo y una plataforma documental completa. | Vigente |
| D-076 | 2026-05-05 | El ciclo de vida minimo documental se modelara en `StoredDocument` con estado `ACTIVE`/`ARCHIVED`, archivado/restauracion logica ADMIN-only, motivo breve opcional y sin borrar ni mover archivos fisicos. | Permite separar documentos vigentes y archivados en el catalogo transversal con un cambio acotado, auditable y reversible, sin abrir retencion avanzada, backup real, versionado ni plataforma documental completa. | Vigente |
| D-077 | 2026-05-05 | Los documentos archivados seguiran siendo descargables para usuarios con permiso de lectura del modulo mientras su integridad sea valida. | En esta etapa `ARCHIVED` significa ocultamiento operativo del catalogo por defecto, no bloqueo legal ni retencion; conserva acceso a evidencia historica sin crear permisos por documento ni reglas no aprobadas. | Vigente |
| D-078 | 2026-05-05 | La clasificacion documental minima se resolvera con codigos constantes en dominio (`CERTIFICATE`, `SIGNED_DOCUMENT`, `SUPPORTING_DOCUMENT`, `PHOTO_EVIDENCE`, `VIDEO_EVIDENCE`, `OTHER`) y metadata simple en `StoredDocument`, sin tabla catalogo ni clasificacion automatica avanzada. | Da filtros operativos utiles y prepara politica documental futura con bajo costo, evitando taxonomia compleja, OCR, reglas inteligentes o edicion granular por documento en esta etapa. | Vigente |
| D-079 | 2026-05-05 | Los uploads nuevos clasificaran por area y content-type: cédulas de Mercados como `CERTIFICATE` principal, evidencias PDF como `SUPPORTING_DOCUMENT`, evidencias JPEG/PNG como `PHOTO_EVIDENCE`; documentos no mapeados conservaran `OTHER`. | Es una regla documentada, deterministica y compatible con los flujos actuales, suficiente para operacion sin inferencia automatica compleja. | Vigente |
| D-080 | 2026-05-05 | La edicion administrativa minima de metadata documental permitira modificar solo `DocumentClassCode`, `BusinessPurpose`, `IsPrimaryDocument` y `ClassificationNotes`, sin tocar rutas, tamano, hash, content-type, modulo, entidad, integridad ni estado. | Hace operable el catalogo transversal sin abrir una plataforma documental completa ni poner en riesgo la integridad/storage ya endurecidos. | Vigente |
| D-081 | 2026-05-05 | La marca de documento principal se limitara a un documento `ACTIVE` por `DocumentAreaCode + EntityType + EntityId`; al marcar uno nuevo, la API desmarca otros principales activos del mismo conjunto. | Da una regla simple, verificable y reversible para operacion documental sin introducir versionado, jerarquias ni workflow complejo. | Vigente |
| D-082 | 2026-05-05 | La retencion minima documental persistira `RetentionPolicyCode` y `RetentionUntilUtc`, pero `RetentionStatusCode` se calculara al consultar usando la fecha actual y una ventana operativa de revision de 30 dias. | Evita un estado persistido que quede obsoleto sin jobs ni scheduler, mantiene filtros utiles y conserva el alcance como metadata operativa, no como motor de cumplimiento documental. | Vigente |
| D-083 | 2026-05-05 | Las politicas iniciales de retencion se derivaran de la clase documental: `CERTIFICATE_REVIEW` a 1 año, `SIGNED_LONG_TERM` a 7 años, `EVIDENCE_MEDIUM_TERM` a 3 años y `GENERIC_REVIEW` a 1 año desde `CreatedUtc`; `EXPIRED_RETENTION` no borra, mueve ni bloquea descargas. | Da una convencion provisional y transparente para operar el catalogo y preparar decisiones futuras, sin abrir borrado automatico, legal hold, matriz normativa compleja ni backup real. | Vigente |
| D-084 | 2026-05-05 | La revision de retencion se modelara como operacion ADMIN-only con estados `REVIEW_PENDING`, `REVIEW_COMPLETED` y `REVIEW_DEFERRED`; diferir solo ajusta `NextRetentionReviewUtc` y marcar revisado no cambia clase, ciclo de vida, politica de retencion, descarga ni storage. | Permite operar documentos `REVIEW_DUE` o `EXPIRED_RETENTION` sin convertir la retencion en workflow legal, sin borrar o mover archivos y sin abrir legal hold, scheduler ni plataforma documental completa. | Vigente |
| D-085 | 2026-05-05 | La navegacion contextual documental se resolvera por DTO y consultas acotadas sobre entidades ya existentes, no con nuevas columnas ni joins transversales complejos. | `StoredDocument` ya contiene `ModuleCode`, `EntityType` y `EntityId`; resolver `OriginContext` para Mercados, Donatarias y Federacion en la API permite hacer operable el catalogo sin migracion, sin full-text y sin plataforma documental completa. | Vigente |
| D-086 | 2026-05-05 | La completitud documental minima se calculara en lectura sobre `StoredDocument` activo y entidades origen, sin nuevas columnas ni workflow de cumplimiento. | `MarketTenant` requiere certificado `CERTIFICATE` activo; `DonationApplication` y `FederationDonationApplication` requieren al menos una evidencia documental activa. Esto deja una senal operativa util sin abrir compliance avanzado, reglas legales finas ni plataforma documental completa. | Vigente |
| D-087 | 2026-05-05 | Las reglas documentales minimas viviran en un registry canónico en codigo, no en condiciones dispersas ni en tablas editables. | Centraliza `ruleCode`, modulo, entidad, area, clases requeridas, cardinalidad minima y mensajes de faltante para completitud/pending queue, sin abrir edicion dinamica, motor de compliance ni plataforma documental completa. | Vigente |
| D-088 | 2026-05-05 | Los requisitos documentales se expondran en contexto mediante una consulta calculada por entidad que reutiliza `DocumentRuleRegistry` y la evaluacion de completitud existente. | Evita duplicar reglas en frontend o crear workflow; permite mostrar requisito, estado, conteo y remediacion minima en Mercados, Donatarias y Federacion sin nuevas tablas ni compliance avanzado. | Vigente |
| D-089 | 2026-05-05 | La remediacion documental directa se resolvera reutilizando uploads existentes y agregando solo el puente faltante para cédula de locatario existente. | Donatarias y Federacion ya tenian endpoints de evidencia por aplicacion; Mercados solo cargaba cédula al crear locatario, por lo que `POST /api/markets/tenants/{tenantId}/cedula` cubre el faltante sin endpoint documental generico ni workflow. La trazabilidad de sustitucion de cédulas queda formalizada despues por D-090. | Vigente |
| D-090 | 2026-05-05 | El reemplazo documental minimo se representara en `StoredDocument` con enlaces directos entre documento anterior y vigente; el anterior queda `ARCHIVED` y superseded, el nuevo queda `ACTIVE` y apunta al reemplazado. | Conserva trazabilidad operativa de sustitucion sin abrir versionado completo, arbol documental ni storage externo; la completitud cuenta solo documentos `ACTIVE` no superseded y los reemplazados siguen descargables con permiso e integridad valida. | Vigente |
| D-091 | 2026-05-05 | La linea de tiempo documental se calculara en lectura combinando `StoredDocument`, eventos documentales en `AuditEvent` y eventos sinteticos minimos para carga inicial/superseded. | Evita migracion y evita inventar una tabla historica paralela; la historia queda util para operacion y replacement traceability sin abrir versionado completo, workflow documental ni plataforma documental avanzada. | Vigente |
| D-092 | 2026-05-05 | La bandeja documental unificada se calculara en lectura componiendo pendientes de completitud, issues de integridad y revision de retencion, sin persistir tareas ni estados de workflow. | Da una superficie operativa unica y filtrable con bajo costo, reutiliza reglas/documentos existentes y evita abrir asignaciones, aprobaciones, acciones masivas o task management antes de tener un workflow aprobado. | Vigente |
| D-093 | 2026-05-05 | El resumen ejecutivo documental se calculara en lectura sobre `StoredDocument` y la composicion de work queue, sin persistir metricas ni introducir una capa de BI. | Da visibilidad rapida de estado documental y priorizacion por modulo con bajo costo, manteniendo la fuente de verdad en catalogo/work queue y evitando analitica pesada, exportaciones complejas o schema change innecesario. | Vigente |
| D-094 | 2026-05-06 | Los overrides de retencion seran ADMIN-only, explicitos y reversibles; la retencion baseline permanece en `RetentionPolicyCode`/`RetentionUntilUtc` y la retencion efectiva usa override cuando existe. | Permite excepciones operativas controladas sin convertir la retencion en legal hold, compliance avanzado ni motor de politicas; review queue, work queue, filtros y summary usan la retencion efectiva para evitar senales inconsistentes. | Vigente |
| D-095 | 2026-05-06 | Establecer o limpiar un override de retencion reinicia la revision operativa a `REVIEW_PENDING`. | Una revision previa puede quedar obsoleta al cambiar la fecha/politica efectiva; resetearla fuerza una re-evaluacion simple sin abrir aprobaciones, workflow ni historial forense adicional. | Vigente |
| D-096 | 2026-05-06 | La validacion runtime de overrides de retencion se cierra en una base SQL aislada con bootstrap local y un `StoredDocument` semilla controlado, sin introducir cambios funcionales nuevos. | Permite comprobar migracion real, permisos por rol, baseline/effective, colas, summary y auditoria sobre la API viva sin contaminar bases previas ni abrir alcance documental adicional. | Aplicada |
| D-097 | 2026-05-06 | El hold administrativo minimo sera ADMIN-only, reversible y distinto de archivado/retencion; no bloquea descarga autorizada y excluye el documento de review queue y de items accionables de retencion en work queue mientras este activo. | Permite congelar temporalmente el tratamiento operativo de retencion sin abrir legal hold formal, compliance avanzado, borrado, workflow ni plataforma documental completa. | Vigente |
| D-098 | 2026-05-06 | El estado operativo documental sera una senal derivada en lectura, no persistida, con precedencia `INTEGRITY_ISSUE`, `ON_HOLD`, `SUPERSEDED`, `ARCHIVED`, `RETENTION_EXPIRED`, `REVIEW_DUE`, `ACTIVE_OK`. | Consolida multiples condiciones relevantes sin crear columnas obsoletas, jobs de recalculo, scoring complejo ni analitica pesada; conserva los campos base para trazabilidad y mantiene la senal sincronizada con integridad, hold, reemplazo, lifecycle y retencion efectiva. | Vigente |
| D-099 | 2026-05-06 | El centro operativo transversal se resolvera por composicion en lectura con entrada `DASHBOARD_READ`, filtrando cada seccion por permisos efectivos y sin persistir metricas. | Reutiliza dashboard, documentos y seguridad ya existentes con bajo costo, evita BI pesado o snapshots analiticos, y mantiene seguridad ADMIN-only fuera de las respuestas para roles sin `USERS_ADMIN`. | Vigente |
| D-100 | 2026-05-06 | La accionabilidad de `/operations` se resolvera con deep links canonicos y metadatos de accion por item; las quick actions se limitaran a operaciones backend existentes, seguras y acotadas, empezando solo por `UNLOCK_USER`. | Permite priorizar y saltar al contexto correcto sin crear workflow, asignaciones, SLA, aprobaciones ni acciones masivas; el backend sigue controlando permisos reales y la UI solo expone acciones cuando el item trae codigo permitido. | Vigente |
| D-101 | 2026-05-07 | La exportacion documental ligera sera CSV por pagina/consulta, reutilizando filtros y permisos existentes de catalogo, work queue y review queue; no incluira rutas fisicas internas ni se convertira en reporte masivo. | Da una salida operativa util y verificable sin abrir BI pesado, XLSX complejo, reportes avanzados, storage externo, backup real ni una plataforma documental completa; mantener el limite `take` existente evita exportaciones masivas accidentales. | Vigente |
| D-102 | 2026-05-07 | Las ventanas temporales de `/operations` se resolveran en lectura con presets `TODAY`, `LAST_7_DAYS`, `NEXT_30_DAYS` y `ALL`; un rango explicito `fromUtc`/`toUtc` tiene precedencia sobre el preset y solo se aplican senales con fecha operativa clara. | Mantiene summary y work queue coherentes sin persistir snapshots ni abrir BI; excluir items sin fecha aplicable evita precision falsa y conserva fuera analitica pesada, scheduler y reporting complejo. | Vigente |
| D-103 | 2026-05-07 | La exportacion ligera de `/operations` sera CSV en lectura, reutilizara la misma composicion de summary/work queue, respetara filtros/ventanas/permisos y mantendra la work queue acotada por `take` normalizado hasta `200`. | Permite compartir pendientes y KPIs operativos fuera de pantalla sin crear reportes pesados, XLSX, snapshots, scheduler ni exportaciones masivas; usar la misma composicion evita divergencia entre UI/API y CSV. | Vigente |
| D-104 | 2026-05-08 | La renovacion minima de `FinancialPermit` se resolvera con una cadena simple sobre el propio permiso: `RenewedFromPermitId`, `CurrentRootPermitId`, `IsCurrentVersion` y `RenewalSequence`; al renovar, el permiso anterior queda historico no vigente y el nuevo queda vigente. | Permite saber cual oficio esta vigente y de cual proviene sin abrir versionado contractual completo, sin sobrescribir el original, sin borrar historicos y sin crear workflow/aprobaciones; las alertas operativas y dashboard financiero consultan solo permisos vigentes para evitar ruido de oficios ya renovados. | Vigente |
| D-105 | 2026-05-08 | La continuidad operativa de Financieras por cadena se resolvera como consulta calculada sobre `CurrentRootPermitId`, sin nueva migracion ni reescritura de creditos/comisiones historicas. | Reutiliza la trazabilidad minima ya persistida, permite ver la linea historica como una sola operacion y mantiene fuera comparativos avanzados, BI, workflow y versionado contractual completo. | Vigente |
| D-106 | 2026-05-08 | Los agregados nominales de comisiones de la cadena usaran codigos ya existentes: `totalPromoterCommission` por `CommissionType.Code=PROMOTER`, `totalAdminCommission` por `CommissionType.Code=ADMINISTRATION` y `totalThirdPartyCommission` por `RecipientCategory=THIRD_PARTY`. | Evita inferir reglas desde nombres libres o contactos, respeta los catalogos existentes de STAGE-02/STAGE-05 y conserva el calculo como resumen operativo minimo hasta que exista una taxonomia mas fina aprobada. | Vigente |
| D-107 | 2026-05-08 | Las altas nuevas de creditos y comisiones de Financieras se permitiran solo cuando el permiso asociado sea el vigente de la cadena (`IsCurrentVersion=true`) y no terminal. | Mantiene la cadena historica consultable sin permitir captura accidental sobre permisos renovados o cerrados, concentra la operacion nueva en el vigente y evita abrir state machine, workflow, aprobaciones o versionado contractual avanzado. | Vigente |
| D-108 | 2026-05-08 | La unicidad operativa minima de permisos de Financieras se resolvera por validacion de aplicacion en alta y renovacion: otro permiso `IsCurrentVersion=true`, no terminal y con la misma combinacion operativa normalizada bloquea la operacion con `FINANCIAL_PERMIT_ACTIVE_CONFLICT`. | Evita dos cadenas vigentes para la misma financiera/dependencia/lugar sin abrir catalogo maestro, workflow ni versionado contractual; no se usa traslape de fechas porque `IsCurrentVersion` ya es la marca operativa vigente y la renovacion valida excluye su propia `CurrentRootPermitId`. | Vigente |
| D-109 | 2026-05-08 | La resolucion del permiso vigente de Financieras se hara como consulta calculada por la misma combinacion operativa normalizada usada para conflicto, devolviendo solo permisos `IsCurrentVersion=true` y no terminales. | Reutiliza la regla ya aprobada para evitar divergencia entre busqueda y bloqueo, reduce busqueda manual en la UI, no requiere migracion ni catalogo maestro y mantiene fuera workflow, versionado contractual y preparacion automatica compleja de captura. | Vigente |
| D-110 | 2026-05-08 | La captura contextual minima de credito de Financieras reutilizara `GET /api/financials/current-permit` y el `POST /api/financials/{permitId}/credits` existente, sin crear endpoint puente. | Mantiene una sola validacion real de captura sobre permiso vigente/no terminal, evita duplicar reglas de alta de credito, permite distinguir no encontrado/historico/terminal desde la resolucion contextual y conserva fuera catalogo maestro, workflow complejo, migracion y auditoria adicional. | Vigente |
| D-111 | 2026-05-08 | `GET /api/financials/current-permit` devolvera una resolucion contextual orientativa para casos no ambiguos, con `suggestionCode` y contexto de siguiente accion en vez de tratar todo caso sin vigente como error HTTP. | Permite orientar al usuario cuando no existe vigente sin abrir workflow: `USE_CURRENT_PERMIT` si hay vigente/no terminal, `RENEW_LAST_PERMIT` si hay antecedente historico no terminal, `REVIEW_TERMINAL_CHAIN` si el antecedente es terminal y `CREATE_NEW_PERMIT` si no hay antecedente; los `POST` existentes conservan la validacion real y la ambiguedad de datos sigue siendo `409`. | Vigente |
| D-112 | 2026-05-11 | La renovacion contextual prellenada se resolvera con `GET /api/financials/{permitId}/renewal-draft` como consulta de lectura, sin entidad draft persistida; si el permiso fuente es historico, el draft prellena desde ese contexto pero apunta la renovacion real al vigente de la cadena. | Permite que `RENEW_LAST_PERMIT` reduzca captura manual sin saltarse la regla de renovar desde el vigente, sin duplicar la resolucion contextual, sin migracion, sin auditoria por preparacion y sin abrir workflow, aprobaciones ni versionado contractual completo. | Vigente |
| D-113 | 2026-05-11 | El alta contextual para `CREATE_NEW_PERMIT` se resolvera solo en la UI prellenando el formulario actual de alta con financiera, dependencia/institucion y lugar/stand de la resolucion contextual; la creacion real seguira usando `POST /api/financials`. | Reduce captura manual cuando no hay vigente ni antecedente, reutiliza la validacion backend de unicidad operativa/estatus/reglas actuales, evita endpoint nuevo, migracion, entidad draft, auditoria de preparacion, catalogo maestro y workflow complejo. | Vigente |
| D-114 | 2026-05-12 | La ficha operativa contextual de Financieras se resolvera como composicion de lectura en `GET /api/financials/context-card`, reutilizando resolucion contextual, cadena y agregados existentes; sus acciones disponibles se derivan de `suggestionCode`. | Permite ver en una sola respuesta el estado operativo del contexto sin duplicar reglas de vigente/historico/terminal/conflicto, sin persistir una entidad de contexto, sin migracion, sin auditoria de consulta y sin abrir catalogo maestro, workflow complejo, aprobaciones, BI ni reportes avanzados. | Vigente |
| D-115 | 2026-05-12 | Donatarias Transparencia Fase 1 se resolvera solo como reorganizacion frontend sobre DTOs y servicios existentes, separando estatus financiero, documental y operativo sin cambiar su significado. | Permite presentar claridad al donante con bajo riesgo, reutilizando `DonationSummary`, `DonationDetail`, aplicaciones, evidencias y catalogo documental actual; mantiene fuera backend nuevo, schema, migraciones, permisos, exportacion formal y validacion legal documental. | Vigente |
| D-116 | 2026-05-12 | Donatarias Transparencia Fase 2 mantendra los nuevos KPIs y la distribucion financiera como calculos frontend derivados de la lista cargada y de la donacion seleccionada, sin endpoint agregado ni schema nuevo. | Permite reforzar transparencia financiera con bajo riesgo y sin tocar contratos; los KPIs de lista se etiquetan como visibles/filtrados, el saldo acumulado por aplicacion depende del orden visual y `CLOSED` sigue separado de aplicacion financiera completa. | Vigente |
| D-117 | 2026-05-12 | Donatarias Transparencia Fase 3 calculara el semaforo documental agregado en backend sobre `DocumentRuleRegistry` y `StoredDocument` activo/no reemplazado, exponiendo solo un DTO de lectura por donacion. | Evita que la UI dependa solo de `evidenceCount`, respeta la capa documental real, mantiene `DONATIONS_READ`, no agrega schema ni migracion, no expone rutas fisicas y conserva el alcance como evidencia minima registrada sin validar suficiencia legal o contable. | Vigente |
| D-118 | 2026-05-12 | Donatarias Transparencia Fase 4 se resolvera como reporte calculado en lectura por donacion mediante `GET /api/donations/{donationId}/transparency-report`, reutilizando el agregado documental de Fase 3. | Da una vista presentable para donante sin persistir snapshot, folio, version oficial ni firma; evita reglas documentales contradictorias, mantiene `DONATIONS_READ`, no cambia schema/permisos y conserva fuera PDF/CSV, aprobacion documental humana y validacion legal avanzada. | Vigente |
| D-119 | 2026-05-13 | Donatarias Transparencia Fase 5A resolvera la salida compartible como vista imprimible frontend con `window.print()`, no como PDF/CSV server-side ni reporte oficial. | Permite compartir visualmente el reporte ya calculado con bajo riesgo y sin abrir backend, schema, migracion, permisos, folio, firma, snapshot/versionamiento ni aprobacion documental; el alcance queda como vista operativa no legal, fiscal ni contable. | Vigente |
| D-120 | 2026-05-13 | El alta maestra `Registrar donacion` de `/donatarias` se movera a modal frontend, reutilizando el formulario, validaciones y servicio existentes, sin cambiar contrato ni endpoint. | Reduce scroll y separa lectura/transparencia de captura sin abrir rediseño completo, wizard, backend, permisos, migracion ni cambios de reglas de negocio; las capturas de aplicacion y evidencia quedan fuera de esta subetapa. | Vigente |
| D-121 | 2026-05-13 | El alta `Registrar aplicacion` de `/donatarias` se movera a modal frontend contextual, reutilizando el formulario, validaciones y servicio existentes, sin cambiar contrato ni endpoint. | Reduce scroll del tab `Aplicaciones / distribucion` y mantiene esa vista enfocada en distribucion financiera, saldo, evidencia y estado documental; `Cargar evidencia` queda fuera de esta subetapa para evitar rediseño completo o cambios de reglas. | Vigente |
| D-122 | 2026-05-13 | La carga `Cargar evidencia` de `/donatarias` se movera a modal frontend contextual, reutilizando formulario, archivo seleccionado, validaciones y servicio existentes, sin cambiar contrato ni endpoint. | Reduce scroll del tab `Evidencias`, enfoca la vista en evidencia registrada y faltantes documentales, y permite remediar aplicaciones pendientes desde su propio contexto sin abrir wizard, backend, permisos, migracion ni rediseño completo. | Vigente |
| D-123 | 2026-05-15 | La pantalla publica `/login` usara un asset local `.webp` en `src/frontend/public/assets/brand/logo-fmcpa.webp` y copy institucional de usuario final, sin mostrar detalles de JWT, bootstrap, variables de entorno ni usuarios sugeridos. | El login debe verse como entrada institucional del sistema, no como nota tecnica de implementacion; mantener el cambio en frontend conserva autenticacion, rutas, guards, contratos API y backend sin riesgo de ruptura funcional. | Vigente |
| D-124 | 2026-05-15 | El shell autenticado usara el mismo logo institucional que login y ocultara detalles tecnicos, rol y acciones permanentes de sesion detras de un menu compacto de usuario. | Alinea la aplicacion autenticada con la identidad institucional y mejora privacidad/usabilidad sin tocar autenticacion, permisos, guards, rutas protegidas, backend ni contratos API; cambio de contraseña y logout siguen disponibles en el dropdown. | Vigente |
| D-125 | 2026-05-15 | La navegacion principal autenticada se agrupara visualmente en `Inicio`, `Operación`, `Control` y `Administración`, ocultando encabezados sin enlaces visibles despues del filtro por permisos. | Convierte la lista tecnica de rutas en una navegacion operativa mas clara sin cambiar rutas, guards, permisos ni contratos; el filtrado sigue dependiendo de `AuthService.hasPermission()` y de la metadata existente por item. | Vigente |
| D-126 | 2026-05-15 | Documents UX Etapa A se resolvera como reorganizacion frontend de copy, densidad, responsive y jerarquia visual, sin cambiar endpoints, contratos, permisos, rutas ni logica documental. | `/documents` y `/documents/work-queue` son superficies transversales; reducir codigos tecnicos y overflow mejora operacion sin asumir cambios de datos. La metadata tecnica se conserva en detalle secundario para diagnostico. | Vigente |
